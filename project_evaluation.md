# Numbers3 予測・分析システム — プロジェクト評価レポート (Ver.2)

**対象:** `repomix-output.xml` (Current Snapshot)  
**日付:** 2026-02-10

---

## 総合スコア（エグゼクティブサマリー）

現在のシステムは、**「実運用可能な高機能プロトタイプ」** の域に達しています。データ収集から分析、予測、可視化、通知までの一連のパイプラインが完成しており、機能面での不足はほぼありません。
しかし、中心となるロジックファイルが肥大化しており、今後の「精度の向上（フェーズA）」や「機能拡張」を行う上での技術的負債が大きくなっています。

| 観点 | 評価 | コメント |
| :--- | :---: | :--- |
| **コード品質** | ⭐⭐☆☆☆ | `numbers3_logic.py` の巨大なモノリス化が深刻なボトルネック。 |
| **アーキテクチャ** | ⭐⭐⭐☆☆ | Streamlit(UI)とLogicの分離はできているが、Logic内部の結合度が強すぎる。 |
| **機能性・ロジック** | ⭐⭐⭐⭐☆ | 統計分析・機械学習・ルールベースを網羅し、バックテスト基盤もある。 |
| **運用・自動化** | ⭐⭐⭐⭐☆ | `auto_update.py` とSlack連携により、完全自動運用が可能なレベル。 |
| **UI/UX** | ⭐⭐⭐⭐☆ | Streamlitを用いて、情報の階層化（タブ、サイドバー）が適切に行われている。 |
| **将来性・拡張性** | ⭐⭐⭐☆☆ | バックテスト環境はあるが、特徴量追加などの実験サイクルを回すにはリファクタリングが急務。 |

---

## 1. コード品質と保守性

### ✅ 良い点
* **型ヒントとドキュメント:** 新しいモジュール（`ml_backtest_runner.py` など）では型ヒントやDocstringが記述されており、可読性が考慮されている。
* **開発ツールの導入:** `tools/check_md.py` など、コード品質以外のドキュメント品質を保つツールも用意されており、丁寧な開発姿勢が見られる。

### ⚠️ 改善が必要な点
* **🔴 `numbers3_logic.py` の責務過多:**
    * この1ファイルに「スクレイピング」「データ加工」「統計計算」「機械学習」「グラフ描画」すべてが含まれていると推測されます。
    * **リスク:** 予測ロジックを少し直すだけで、アプリ全体が動かなくなるリスクがあります。また、単体テスト（Unit Test）を書くのが極めて困難です。
* **ハードコーディング:** 一部のパラメータ（期間設定など）がコード内に直接書かれている箇所があり、実験のたびにコード修正が必要です。

---

## 2. 機能性と予測ロジック (Machine Learning)

### ✅ 良い点
* **ハイブリッドなアプローチ:** 単なるLightGBMだけでなく、統計的な「インターバル」や「パターン」分析を組み合わせている点は、純粋なMLアプローチよりも実戦的です。
* **バックテスト基盤の確立:** `ml_backtest_runner.py` が実装されており、CLI（コマンドライン）からパラメータを変えて検証できる設計は非常に優秀です。

### ⚠️ 改善が必要な点
* **🟡 特徴量のブラックボックス化:**
    * 「なぜその数字が予測されたのか」を説明する機能（Feature Importance）が不足しています。これにより、モデルの改善指針が立てにくくなっています。
* **履歴管理の欠如:**
    * バックテストの結果が上書き、または散逸しがちです。時系列での精度推移（Performance Tracking）を記録する仕組みが必要です。

---

## 3. UI/UX (Streamlit)

### ✅ 良い点
* **キャッシュ戦略:** `@st.cache_data` を適切に使用しており、重いデータ処理が走ってもUIの応答速度を落とさない工夫がされています。
* **情報の整理:** 「インターバル分析」「トレンド分析」「AI予測」などがタブで分かれており、ユーザーが見たい情報にすぐアクセスできます。

### ⚠️ 改善が必要な点
* **信頼度の可視化:** ユーザーにとって最も重要なのは「予測数字」そのものよりも、「その予測にどれくらい自信があるか」です。現在は数字の羅列になっている可能性が高く、確率（Confidence Score）の表示が待たれます。

---

## 4. 運用とセキュリティ

### ✅ 良い点
* **環境変数による管理:** SlackのWebhook URLなどを `os.getenv` で取得しており、セキュリティの基本（Secretの分離）が守られています。
* **安全なスクレイピング:** サーバーへの負荷を考慮した `sleep` 処理が入っており、Banされるリスクを低減しています。

---

## 5. 推奨ロードマップ (Next Steps)

現在の評価に基づき、優先度順に取り組むべきタスクを提示します。

### 【フェーズA】 予測精度の「見える化」と改善（最優先）
*コードを書き換える前に、まず「現状の実力」を正確に把握するフェーズです。*

1.  **特徴量重要度の可視化:** LightGBMがどのデータを重視しているかグラフ化する機能を追加する。
2.  **精度トラッキングの実装:** バックテスト実行時に、結果を `ml_performance_tracking.csv` に追記保存し、改善の推移を追えるようにする。
3.  **特徴量エンジニアリング:** 「曜日」「六曜」「月齢」などのカレンダー要因や、異なる期間（短期5回、長期100回）の移動平均を追加する。

### 【フェーズB】 構造改革（リファクタリング）
*フェーズAで実験サイクルを回しやすくするために、足場を固めます。*

1.  **ディレクトリ分割:** `numbers3_logic.py` を以下の単位で分割する。
    * `src/data/loader.py` (データ取得・更新)
    * `src/features/builder.py` (特徴量生成)
    * `src/models/lgbm.py` (学習・予測)
    * `src/viz/charts.py` (グラフ描画)

### 【フェーズC】 アプリケーションの高度化
*ユーザー体験（UX）を向上させます。*

1.  **AI予測ダッシュボード:** Streamlit上に「AI予測の根拠（重要度）」と「確率分布」を表示する専用タブを作成する。
2.  **アラート機能:** 「当選確率が80%を超えた場合のみSlack通知する」といった閾値設定を実装する。

---

## 結論

プロジェクトは**「0から1」のフェーズを完全にクリア**しています。
現在は「1から10」へスケールさせるための過渡期にあり、**「ロジックファイルの分割」と「予測プロセスの透明化」** が成功の鍵を握っています。これらを行うことで、単なるツールから、継続的に利益を生み出す「資産」へと進化するでしょう。